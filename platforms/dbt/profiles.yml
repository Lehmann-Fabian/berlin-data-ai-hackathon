db_team_9:
  outputs:
    dev:
      type: snowflake
      account: '{{ env_var("DBT_ACCOUNT") }}'
      user: '{{ env_var("DBT_USER") }}'
      password: '{{ env_var("DBT_PASSWORD") }}'
      database: DB_TEAM_9         # ← your team's private database (e.g. DB_TEAM_1)
      schema: base
      warehouse: COMPUTE_WH
      threads: 4
  target: dev
